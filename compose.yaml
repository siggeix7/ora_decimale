services:
  web:
    build: .
    container_name: ora_decimale
    restart: unless-stopped
    ports:
      - "8888:8888"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8888/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
